# Fix Onboarding Modal & Profile API

### Step 1: Plan Confirmation
User approved. Proceeding with implementation.

### Step 2: Create New Component - components/OnboardingModal.tsx
**Status:**
- [ ] Extract IIFE modal code into standalone component
- [ ] Fix Rules of Hooks violation
- [ ] Export the component

**Summary:** Created a dedicated OnboardingModal component that handles the full 3-step onboarding flow outside of the parent component’s JSX scope. All hooks now called at top-level. Props: `isOpen`, `user`, `onFinish`.

---

### Step 3: Clean & Fix app/dashboard/layout.tsx
**Status:**
- [ ] Remove IIFE modal from JSX return
- [ ] Remove dead onboarding state (`onboardingInput`, `onboardingError`, `handleOnboardingSubmit`)
- [ ] Import and conditionally render `<OnboardingModal />`

**Summary:** Removed the problematic IIFE from the return body and replaced it with the new `<OnboardingModal />` component, rendering based on `isSubdomainEmpty` flag. Deleted unused `onboardingInput`, `onboardingError` and `handleOnboardingSubmit`.

---

### Step 4: Fix app/api/profile/route.ts
**Status:**
- [ ] Expand `PUT` handler to accept `fullName` and `profession`
- [ ] Persist `fullName` and `profession` during `upsert`

**Summary:** Server-side `PUT` handler was only persisting `subdomain`; updated to accept additional fields from the final step of the onboarding flow so that `fullName` and `profession` are written to the database.

---

