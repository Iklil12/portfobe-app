import React from 'react';

export const LogoIcon = ({ className = "w-10 h-10" }: { className?: string }) => {
  return (
    <svg 
      viewBox="0 0 100 100" 
      fill="none" 
      xmlns="http://www.w3.org/2000/svg"
      className={className}
    >
      {/* 1. Lingkaran Luar (Ring Hitam/Slate) */}
      <circle cx="50" cy="50" r="40" stroke="#0f172a" strokeWidth="12" />
      
      {/* 2. Lensa Dalam - Setengah Atas (Oranye Portfo) */}
      <path d="M 28 50 A 22 22 0 0 1 72 50 Z" fill="#ff9e00" />
      
      {/* 3. Lensa Dalam - Setengah Bawah (Hitam/Slate) */}
      <path d="M 28 50 A 22 22 0 0 0 72 50 Z" fill="#0f172a" />
      
      {/* 4. Garis Irisan Tengah (Putih) - Menciptakan efek potongan */}
      <rect x="5" y="47" width="90" height="6" fill="#ffffff" />
      
      {/* 5. Pupil/Celah Tengah (Putih) */}
      <circle cx="50" cy="50" r="5" fill="#ffffff" />
    </svg>
  );
};